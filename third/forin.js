const object={
    js:"java",
    py:"python"
}
for (const ob in object)
    {
        console.log(ob)
    }
    for (const ob in object)
        {
            console.log( ob, ":-",object[ob])
        }


        